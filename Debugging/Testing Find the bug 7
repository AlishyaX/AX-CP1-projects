#Alishya Xavier, Find the Bug 7

def sum_even_numbers(numbers):
    sum = 0
    for num in numbers:
          if num % 2 == 0:
              sum += num
          else:
              return 0
    return sum
